#include <stdio.h>
#include "main.h"
/**
 * add(int i, int j) - sums two inputed integers
 * @int i: int i is the input number 1
 * @int j: int j is the sceond input
 *
 * Return: the addition of two integers always
 */
int add(int i, int j)
{
	int n;
	n = i + j;
}
	return (n);
}
