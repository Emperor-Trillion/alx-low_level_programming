#include "main.h"

/**
 * print_numbers - prints set of numbers
 * takes not input
 *
 * Return: returns no output
 */
void print_numbers(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
}
